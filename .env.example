# Environment Configuration
# Copy this file to .env and fill in your values

# Node.js Configuration
NODE_NO_WARNINGS=1

# Runtime Environment (local or cloud)
RUN_ENV=local

# Stage/Environment name
STAGE=dev

# API Configuration
BASE_DOMAIN=http://localhost:3000

# Database Configuration (for local development)
DATABASE_URL=sqlite:///nwassiktest.db

# AWS Configuration (only needed for cloud deployment)
AWS_REGION=eu-west-3
AWS_PROFILE=your-aws-profile-name
# DATABASE_SECRET_NAME=rds!nwassik-dev-app-db-secret

# AWS Cognito Configuration (required for deployment)
# Get these values from terraform outputs in ../nwassik-infra:
#   cd ../nwassik-infra && terraform output cognito_user_pool_id
#   cd ../nwassik-infra && terraform output cognito_app_client_id
COGNITO_USER_POOL_ID=eu-west-3_XXXXXXXXX
COGNITO_APP_CLIENT_ID=XXXXXXXXXXXXXXXXXXXXXXXXXX

# API Gateway Endpoint (updated after serverless deploy)
API_ENDPOINT=https://your-api-id.execute-api.eu-west-3.amazonaws.com

# Application Limits
MAX_USER_CREATED_REQUESTS=20
MAX_USER_CREATED_FAVORITES=100
